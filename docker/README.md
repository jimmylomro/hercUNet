# docker/

A **single unified image** for the whole pipeline — all stages share the same code, so the role is just which entrypoint/command you run. Populated in a later stage. See [docs](../docs/README.md).
