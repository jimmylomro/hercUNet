"""Per-stage visual tools (SKELETON — not yet ported).

Each pipeline stage ships a visual tool that shows what it does (the "see what it does"
requirement). Ported alongside each stage; see ``docs/README.md``.
"""
