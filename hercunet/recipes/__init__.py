"""Shipped HercUNet training recipes (YAML). The default is ``hercunet.yml`` (the run301 / v0 recipe),
loaded by :mod:`hercunet.training.recipe`. Copy one and pass it to ``hercunet train fit --recipe <file>``
to run a variant.
"""
