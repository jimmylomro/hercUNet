"""STAGE 3 — single-instance full-volume inference (SKELETON — not yet ported).

Planned: tile+halo scheduling over the data layer, OME-Zarr surface-probability pyramid out
(VC3D-compatible), verified byte-faithful vs official nnU-Net. Ported in a later stage; see
``docs/README.md``.
"""
