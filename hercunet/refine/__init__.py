"""STAGE 2 — the iterative HercUNet refiner (SKELETON — not yet ported).

Planned: the 8-ch ``[CT, prev, orient6]`` affinity/MALIS trainer with material supervision and
DAgger self-rollouts, plus the N-pass iterative inference wrapper. Ported in a later stage;
see ``docs/README.md``.
"""
